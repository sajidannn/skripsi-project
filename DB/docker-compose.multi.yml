services:
  db:
    image: postgres:16
    container_name: pg-pos-multi
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: supersecret
      POSTGRES_DB: pos_master
    ports:
      - "5433:5432"
    volumes:
      - pgdata_multi:/var/lib/postgresql/data
      - ./pos-multi-db-master.sql:/docker-entrypoint-initdb.d/pos-multi-db-master.sql
      - ./pos-multi-db-tenant.sql:/docker-entrypoint-initdb.d/pos-multi-db-tenant.sql
      - ./seed-multi-db-master.sql:/docker-entrypoint-initdb.d/seed-multi-db-master.sql
      - ./seed-multi-db-tenant.sql:/docker-entrypoint-initdb.d/seed-multi-db-tenant.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d pos_master" ]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata_multi:
