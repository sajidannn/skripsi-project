services:
  api:
    build: .
    container_name: pos-api-multi
    environment:
      DB_MODE: multi
      MASTER_DSN: postgres://postgres:supersecret@10.104.0.4:5433/pos_master?sslmode=disable
      TENANT_DB_HOST: 10.104.0.4
      TENANT_DB_PORT: "5433"
      PORT: "8080"
      JWT_SECRET: change-me-in-production
    ports:
      - "8080:8080"
    restart: on-failure
